<svg fill="none" viewBox="0 0 800 240" width="800" height="240" xmlns="http://www.w3.org/2000/svg">
    <foreignObject width="100%" height="100%">
        <div xmlns="http://www.w3.org/1999/xhtml">
            <style>
                :root {
                    /* Nord Color Tokens - Warmer Variant */
                    --color-bg: #2e3440;
                    --color-surface: #3b4252;
                    --color-accent: #8fbcbb;
                    --color-warning: #d08770;
                    --color-on-surface: #d8dee9;
                    --color-danger: #bf616a;
                    --color-success: #a3be8c;
                    --nord-yellow: #ebcb8b;

                    /* Typography */
                    --font-mono: 'JetBrains Mono', ui-monospace, 'SF Mono', Monaco, monospace;

                    /* Spacing Tokens */
                    --space-2: 8px;
                    --space-3: 12px;
                    --space-4: 16px;
                    --space-5: 24px;
                    --space-6: 32px;

                    /* Border Radius */
                    --radius-md: 8px;
                    --radius-lg: 12px;
                }

                * { margin: 0; padding: 0; box-sizing: border-box; }
                .container {
                    width: 800px;
                    height: 240px;
                    background: linear-gradient(135deg, var(--color-bg) 0%, var(--color-surface) 100%);
                    border-radius: var(--radius-lg);
                    padding: var(--space-5) var(--space-6);
                    font-family: var(--font-mono);
                }
                .terminal-header {
                    display: flex;
                    align-items: center;
                    gap: var(--space-2);
                    padding: var(--space-2);
                    background-color: rgba(46, 52, 64, 0.5);
                    border-radius: var(--radius-md);
                    margin-bottom: var(--space-5);
                }
                .circle {
                    width: 12px;
                    height: 12px;
                    border-radius: 50%;
                }
                .red { background: var(--color-danger); }
                .yellow { background: var(--nord-yellow); }
                .green { background: var(--color-success); }

                .command-line {
                    display: flex;
                    align-items: center;
                    gap: var(--space-3);
                    margin-bottom: var(--space-4);
                    font-size: 20px;
                    opacity: 0;
                    animation: fadeIn 0.3s ease-out forwards;
                }
                .arrow {
                    color: var(--color-warning);
                    font-weight: bold;
                }
                .spinner {
                    color: var(--color-warning);
                    font-size: 18px;
                }
                .spinner::before {
                    content: '⣾';
                    animation: rotate 1.2s linear infinite, complete 0s 5.0s forwards;
                }
                @keyframes rotate {
                    0% { content: '⣾'; }
                    12.5% { content: '⣽'; }
                    25% { content: '⣻'; }
                    37.5% { content: '⢿'; }
                    50% { content: '⡿'; }
                    62.5% { content: '⣟'; }
                    75% { content: '⣯'; }
                    87.5% { content: '⣷'; }
                    100% { content: '⣷'; }
                }
                @keyframes complete {
                    to { content: '✓'; color: var(--color-success); }
                }
                .command {
                    color: var(--color-accent);
                    overflow: hidden;
                    border-right: 2px solid var(--color-accent);
                    white-space: nowrap;
                    width: 0;
                    animation:
                        typing 1s steps(6) 0.3s forwards,
                        blink 0.75s step-end 0s 2,
                        hideCursor 0s 1.5s forwards;
                }
                @keyframes typing {
                    from { width: 0; }
                    to { width: 5.5em; }
                }
                @keyframes blink {
                    50% { border-color: transparent; }
                }
                @keyframes hideCursor {
                    to { border-color: transparent; }
                }

                .output {
                    display: flex;
                    flex-direction: column;
                    gap: var(--space-2);
                    font-size: 16px;
                }
                .line {
                    display: flex;
                    gap: var(--space-3);
                    align-items: flex-start;
                    opacity: 0;
                }
                .line:nth-child(1) {
                    animation: fadeInUp 0.5s ease-out 1.3s forwards;
                }
                .line:nth-child(2) {
                    animation: fadeInUp 0.5s ease-out 1.8s forwards;
                }
                .line:nth-child(3) {
                    animation: fadeInUp 0.5s ease-out 4.5s forwards;
                }
                .bullet {
                    color: var(--color-warning);
                    flex-shrink: 0;
                }
                .text {
                    color: var(--color-on-surface);
                    line-height: 1.6;
                }
                .highlight {
                    color: var(--color-accent);
                    font-weight: 500;
                }
                .typing-line {
                    overflow: hidden;
                    white-space: nowrap;
                    width: 0;
                    animation: typeLine 2.5s steps(50) 2.0s forwards;
                }
                @keyframes typeLine {
                    to { width: 100%; }
                }
                a {
                    color: var(--color-accent);
                    text-decoration: none;
                }
                a:hover {
                    text-decoration: underline;
                }
                @keyframes fadeIn {
                    to { opacity: 1; }
                }
                @keyframes fadeInUp {
                    from {
                        opacity: 0;
                        transform: translateY(10px);
                    }
                    to {
                        opacity: 1;
                        transform: translateY(0);
                    }
                }
            </style>
            <div class="container">
                <div class="terminal-header">
                    <div class="circle red"></div>
                    <div class="circle yellow"></div>
                    <div class="circle green"></div>
                </div>
                <div class="command-line">
                    <span class="arrow">&gt;</span>
                    <span class="spinner"></span>
                    <span class="command">whoami</span>
                </div>
                <div class="output">
                    <div class="line">
                        <span class="bullet">⏺</span>
                        <span class="text">
                            <span class="highlight">Daryl Lundy</span> – Aspiring DevSecOps Engineer
                        </span>
                    </div>
                    <div class="line">
                        <span class="bullet">⏺</span>
                        <span class="text typing-line">Designing, building, and securing production systems</span>
                    </div>
                    <div class="line">
                        <span class="bullet">⏺</span>
                        <span class="text">
                            Portfolio: <a href="https://www.daryllundy.com" target="_blank" class="highlight">www.daryllundy.com</a>
                        </span>
                    </div>
                </div>
            </div>
        </div>
    </foreignObject>
</svg>
